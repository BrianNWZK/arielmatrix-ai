{
  "version": 2,
  "builds": [
    {
      "src": "api/cosmoweb3db.py",
      "use": "@vercel/python",
      "config": {
        "runtime": "python3.11",
        "installCommand": "apt-get update && apt-get install -y libsodium-dev cmake build-essential && pip install --upgrade pip && pip install -r requirements.txt"
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/cosmoweb3db.py"
    }
  ]
}
