{
  "version": 2,
  "builds": [
    {
      "src": "api/cosmoweb3db.py",
      "use": "@vercel/python",
      "config": {
        "runtime": "python3.11",  // Downgrade to 3.11 for better compatibility
        "installCommand": "apt-get update && apt-get install -y libsodium-dev && pip install --upgrade pip setuptools wheel && pip install -r requirements.txt"
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/cosmoweb3db.py"
    }
  ]
}
